#include<conio.h>
#include<stdio.h>
#include<string.h>
int main()
{
char arr[20];
printf("enter a string to reverse");
gets(arr);
strrev(arr);
printf("reverse of entered string is %s\n",arr);
reeturn 0;
}
